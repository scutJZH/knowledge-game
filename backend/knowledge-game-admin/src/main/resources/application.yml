server:
  port: 8081

cors:
  allowed-origins: http://localhost:*

spring:
  application:
    name: knowledge-game-admin
  config:
    import:
      - nacos:common.yml?refresh=true
      - nacos:knowledge-game-admin.yml?refresh=true
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      discovery:
        namespace: ${NACOS_NAMESPACE:}
      config:
        namespace: ${NACOS_NAMESPACE:}
